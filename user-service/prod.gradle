apply plugin: 'org.graalvm.buildtools.native'

graalvmNative {
    metadataRepository {
        enabled = true
    }
    registerTestBinary("integrationTest") {
        usingSourceSet(sourceSets.integrationTest)
        forTestTask(tasks.named("integrationTest", Test))
    }
}
