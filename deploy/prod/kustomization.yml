namePrefix: prod-

bases:
  - ../base

patchesStrategicMerge:
  - user-service.yml

secretGenerator:
- name: secrets-env
  envs:
  - .env
  - .env.public

- name: config-files
  files:
  - rsa.private.key
  - rsa.public.key
